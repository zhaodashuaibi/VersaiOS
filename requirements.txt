# VersaiOS Python 依赖清单
# 安装：python -m pip install -r requirements.txt
# 所有版本均经过本地/CI 验证，请勿随意升级 major 版本。

# ========== 核心 CLI / 主控端（src/main_versaios.py） ==========
pyserial>=3.5,<4.0

# 视觉大模型 SDK：仅需 OpenAI 兼容接口
openai>=1.0.0,<2.0.0

# ========== 视觉处理（src/vision_engine.py） ==========
# numpy 1.x 没有 Python 3.13  wheels，必须允许 2.x
numpy>=1.26.0,<3.0.0
# opencv-python 4.10+ 开始提供 Python 3.13 预编译包
opencv-python>=4.10.0,<5.0.0
Pillow>=10.0.0,<12.0.0
mss>=9.0.0,<11.0.0
pygetwindow>=0.0.9,<1.0.0

# ========== 图形化控制台（gui_app.py） ==========
customtkinter>=5.2.0,<6.0.0
